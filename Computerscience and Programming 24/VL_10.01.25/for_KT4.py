# Mittelwert
# Median
# Varianz ausrechnen
# from scratch implementierung von mittelwert
# funktionsdefinitionen & argsort

import numpy as np
x = [3, 4, 8, 9]
for element in x:
    average = sum(x) / len(x)
    
print(average)

print(np.mean(x))